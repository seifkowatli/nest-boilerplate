export enum FileTypes { 
    JSON = 'json',
    CSV = 'csv',
    PDF = 'pdf',
    EXCEL = 'excel',
    POWERPOINT = 'powerpoint',
    WORD = 'word',
    VIDEO = 'video',
    IMAGE = 'image',
}


export enum Entities {
    USERS = 'users',
    TASKS = 'tasks',
}